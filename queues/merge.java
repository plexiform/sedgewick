public class merge {
    private static void merge(Comparable[] a, Comparable[] aux, int lo, int mid, int hi) {
        for (int i = 0; i < hi; i++) {
            aux[i] = a[i];
        }

        int j = lo;
        int k = mid + 1;

        for (int i = lo; i < mid; i++) {
            if (i > mid)
        }

    }
}
